source "https://rubygems.org"

# 使用 github-pages gem，自动对齐 GitHub Pages 的所有依赖版本
gem "github-pages", group: :jekyll_plugins

# Windows 平台时区支持（本地若有需要）
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
